{
  "name": "osc-documentation",
  "version": "0.1.0",
  "description": "Documentation for the Open Science Collective",
  "scripts": {
    "setup": "uv sync",
    "dev": "uv run mkdocs serve",
    "build": "uv run mkdocs build",
    "deploy": "uv run mkdocs build && wrangler pages deploy site --project-name=osc-docs"
  },
  "license": "BSD-3-Clause",
  "repository": {
    "type": "git",
    "url": "https://github.com/OpenScience-Collective/documentation"
  }
}
